server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file://${user.dir}/../config-repo
          clone-on-start: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
